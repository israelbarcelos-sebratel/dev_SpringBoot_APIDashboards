# Copy to .env (or export these in your shell / IDE run config) — never commit real values.
DB_HOST=10.0.11.171
DB_PORT=3306
DB_NAME=API_WebDeveloper
DB_USER=DevSebratel
DB_PASSWORD=changeme
SERVER_PORT=8092
BFF_ORIGIN=http://localhost:4002
